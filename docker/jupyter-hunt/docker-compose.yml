version: '3.5'

services:
  jupyter-hunt:
    image: cyb3rward0g/jupyter-hunt:0.0.1
    container_name: jupyter-hunt
    volumes:
      - notebooks:/opt/helk/jupyter/notebooks
    environment:
      JUPYTER_TYPE: lab
      JUPYTER_BASE_URL: /jupyter
    ports:
      - "8888:8888"
    restart: always
    networks:
      hunting:

networks:
  hunting:
    driver: bridge

volumes:
  notebooks:
    driver: local